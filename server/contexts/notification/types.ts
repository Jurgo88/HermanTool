// Domain types for Notification [MVP · generic]. Empty placeholder —
// reserved so the context has an obvious home for its published
// interface types (Part 1 §4, §5 Ubiquitous Language) when the next
// issue implements them. See ./index.ts for the context's boundary and
// citations.
export {}
