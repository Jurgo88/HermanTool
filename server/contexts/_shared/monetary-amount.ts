// Reserved for MonetaryAmount (D-21, Part 3 §12).
//
// Every monetary amount in this system carries its own currency — never a
// bare number. Not implemented yet; this file marks where the shared
// value type will live once a context needs it, so the concept has a
// place before the first amount is written instead of after.
export {}
